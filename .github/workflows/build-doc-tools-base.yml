name: Build document tools base image

on:
  workflow_dispatch:
  push:
    paths:
      - Dockerfile.base
      - .github/workflows/build-doc-tools-base.yml

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push reusable base image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.base
          push: true
          tags: |
            ghcr.io/ferdousbhuiya/pdf-doc-tools-base:py311
            ghcr.io/ferdousbhuiya/pdf-doc-tools-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify anonymous pull access
        run: |
          docker logout ghcr.io || true
          docker image rm ghcr.io/ferdousbhuiya/pdf-doc-tools-base:py311 || true
          docker pull ghcr.io/ferdousbhuiya/pdf-doc-tools-base:py311
          docker run --rm ghcr.io/ferdousbhuiya/pdf-doc-tools-base:py311 sh -lc 'python --version && libreoffice --version && pdftoppm -v 2>&1 | head -1 && tesseract --version | head -1 && gs --version && latex --version | head -1 && dvipng --version | head -1'
